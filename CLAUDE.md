# FABEL5 (football-scraper) — อ่านก่อนแตะโค้ด

คู่มือรวม: `D:\Projects\CLAUDE.md` · ความจำ: `_agent\memory\project-fabel5.md` (ยาว อ่านก่อนแก้ของจริง)
GitHub: **`YedDekM2-Ui/football-scraper`**

## คืออะไร

**"Forebet ติดหนี้ประตู + เวลาใกล้หมด = เตือน"** — ตัวเฝ้าราคา/สถานการณ์บอลสด
🔴 **ไม่ใช่ตัวทำนายผล และไม่ใช่ตัวแนะนำแทง** — ห้ามเปลี่ยนเจตนานี้

**ทางเดิน:** GitHub Actions `fb-cache` (วันละ 4 รอบ ดึง Forebet อ้อม `?ff=` ผ่าน PIKTAX) → แคช → `fb-watch` (ทุก 5 นาที อ่านแคช + ยิง LiveScore เอง) → ยิงเข้า PIKTAX `action=notify` → Telegram
เกรดผลย้อนหลังทำบน GAS `Fabel5.gs` ในโปรเจกต์ PIKTAX

**เครื่องที่ 4 (แยกขาด ไม่มีท่อส่งข้อความ):** `fb-report` → `fb_report.py` → GitHub Pages
`https://yeddekm2-ui.github.io/football-scraper/` · ในนั้นมี **สถิติออโต้** `fb_stat.py`:
จดใบ**ก่อนบอลเตะ**ลง `stats/picks.jsonl` → พอผลออกเกรดเอง → workflow คอมมิตแฟ้มกลับเข้า repo
🔒 ห้ามจดย้อนหลัง (เท่ากับเลือกใบทีหลังตอนรู้ผล) · ห้ามย้ายแฟ้มไป runner/เครื่องบ้าน

---

## กฎเหล็ก

1. **4 ไฟล์ต้องตรงกันเป๊ะ** (ชื่อตลาด · เกณฑ์ · ค่า threshold): `fb_calib.py` · `fb_watch.py` · `fb_grade.py` · `Fabel5.gs` (อยู่ในโฟลเดอร์ PIKTAX) — แก้ตัวเดียวแล้วลืมตัวอื่น = สถิติเพี้ยนเงียบๆ
2. **ห้ามเตือนก่อนเกมเริ่ม** — เตือนเฉพาะบอลที่กำลังเล่นอยู่
3. **ห้ามตัด `fb_hist.jsonl` ทิ้ง** (คลัง 132,851 คู่ / 365 วัน) — วัดแล้ว เหลือ 90 วันด่านลีกเหลือ 1 ลีกจาก 30
4. **ห้ามลด `NAME_MIN = 0.80`** ใน `live_api.py` (ด่าน fail-closed จับคู่ชื่อทีม)
5. **ห้ามใส่ `TELEGRAM_BOT_TOKEN` ใน env ของ workflow `fb-watch`** — มันยิงผ่าน PIKTAX อย่างเดียว
6. **ห้ามเปิดตัวเฝ้าที่เครื่องบ้านพร้อมกับ Actions** — ยิงซ้ำ (ปิดที่บ้านไปแล้ว)

---

## ตลาดที่ใช้จริง 4 ตัว (เส้นฐาน 78.4%)

`lv_nolose` 89.8% · `bh_g1` 86.1% · `bts1_g1` 84.0% · `over_g1` 82.7%
วัดเป็น **"ลูกที่ยังไม่เกิด"** ทั้งหมด · ตัวที่ฆ่าทิ้งแล้ว: `ht00_2h_goal`, `debt_*` — อย่าเอากลับมาโดยไม่วัดใหม่

**หน้าต่างเตือน:** `MIN_FROM, MIN_TO = 45, 47` (เฉพาะช่วงพักครึ่ง — LiveScore ค้างนาที 45 ตลอด ~15 นาที)
**ป้ายบนการ์ด:** ทุกท่อนที่พูดถึงผลอีกแบบ **ต้องมีเลข % ของตัวเอง** (เช่นบรรทัด `📎` ของ `bh_g1`)

---

## กับดัก

- **Forebet บล็อก IP ของ GitHub Actions (403)** → ต้องอ้อมผ่าน PIKTAX `?ff=` เท่านั้น (LiveScore ยิงตรงได้)
- เลข `Forebet 585` ในล็อก = **3 วันรวม** ห้ามอ่านเป็นจำนวนคู่รายวัน
- **ห้ามเขียน `*/<ตัวเลข>` ในคอมเมนต์ `/* */` ของ GAS** — ปิดคอมเมนต์กลางคัน
- ของที่เก็บไว้ **วัดทีหลังต้องจบที่ Google Sheet** — ไฟล์ในเครื่อง/บน runner หายทุกรอบ

## ค้างอยู่

- คอลัมน์ U `นาทีที่เห็นลูก` (`f5stamp`) เก็บได้ไม่ครบ → ใส่ตัวนับบอกเหตุผลไว้แล้ว รอเก็บ 2-3 อาทิตย์ค่อยสรุป
- ใบแดงจาก goaloo (`[13]`/`[14]`) จดลงคอลัมน์ T แล้ว **ยังไม่เอามาตัดสิน** (ไม่มีข้อมูลย้อนหลังให้วัด)
